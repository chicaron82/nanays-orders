// Makes the @testing-library/jest-dom matchers (toBeInTheDocument, etc.) visible
// to TypeScript in the component tests. The runtime side is wired in tests/setup.js.
import '@testing-library/jest-dom/vitest';
